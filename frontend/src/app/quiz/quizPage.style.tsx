import styled from '@emotion/styled';

export const QuizContainer = styled.div`
    padding: 5px;
`

export const QuizButtonList = styled.div`
    display: flex;
    flex-direction: row;
    gap: 10px;
`
